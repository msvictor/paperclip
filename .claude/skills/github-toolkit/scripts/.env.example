# Copy to .env (or export directly in your shell) before using write/API commands.
# Required: a GitHub token with repo scope (fine-grained tokens: Contents R/W,
# Pull requests R/W on the target repositories).
GITHUB_TOKEN=<your github token>